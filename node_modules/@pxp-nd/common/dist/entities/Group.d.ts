import { BaseEntity } from 'typeorm';
import User from './User';
export default class Group extends BaseEntity {
    groupId: number;
    name: string;
    code: string;
    branchId: string;
    users: User[];
}
