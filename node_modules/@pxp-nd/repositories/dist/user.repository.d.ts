import { Repository } from 'typeorm';
import { User } from '@pxp-nd/entities';
export declare class UserRepository extends Repository<User> {
    findOrCreateSocial(options: any, data: any): Promise<User>;
}
