import { Repository } from 'typeorm';
import { AccountStatus } from '@pxp-nd/entities';
export interface StatusData {
    accountStatus: any;
    accountStatusType: any;
}
declare class AccountStatusCustomRepository extends Repository<AccountStatus> {
    saveStatus(data: StatusData, currentAccountId?: any): Promise<any | null>;
    accountBalance(tableId: number, code: string): Promise<any | null>;
    add(params: any, user: any): Promise<unknown>;
}
export declare const accountStatusRepository: () => AccountStatusCustomRepository;
export {};
