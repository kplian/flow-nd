import { EntitySubscriberInterface, InsertEvent } from 'typeorm';
import { WordKey } from '@pxp-nd/entities';
export declare class WordKeySubscriber implements EntitySubscriberInterface<WordKey> {
    listenTo(): typeof WordKey;
    afterInsert(event: InsertEvent<WordKey>): Promise<void>;
}
