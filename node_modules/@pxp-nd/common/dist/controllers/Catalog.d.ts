import { Controller } from '@pxp-nd/core';
import { Catalog as CatalogModel } from '../entities';
declare class Catalog extends Controller {
    find(params: Record<string, unknown>): Promise<any>;
    findById(params: Record<string, unknown>): Promise<CatalogModel | any>;
    save(params: any): Promise<CatalogModel>;
    edit(params: any): Promise<any>;
    delete(params: any): Promise<any>;
}
export default Catalog;
