import { Socket, Server } from 'socket.io';
export declare const usersConnected: any;
export declare const connectClient: (client: Socket, io: Server) => void;
export declare const disconnect: (client: Socket, io: Server) => void;
export declare const message: (client: Socket, io: Server) => void;
