import Role from './Role';
import Subsystem from './Subsystem';
import UiTransaction from './UiTransaction';
import { PxpEntity } from '../PxpEntity';
export default class Ui extends PxpEntity {
    uiId: number;
    code: string;
    name: string;
    description: string;
    visible: string;
    route: string;
    order: number;
    icon: string;
    roles: Role[];
    transactions: UiTransaction[];
    parentId: number;
    subsystemId: number;
    parent: Ui;
    children: Ui[];
    subsystem: Subsystem;
    roleId: number;
    type: string;
    static findRecursive(params: Record<string, unknown>, parentId: number, isAdmin?: boolean, uiList?: number[]): Promise<unknown>;
}
